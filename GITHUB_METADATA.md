# GitHub Repository Metadata — FashionistaAI

Use these values in the GitHub **About** section (gear icon on the repository homepage) for [cloudinary-devs/Cloudinary-FashionistaAI](https://github.com/cloudinary-devs/Cloudinary-FashionistaAI).

## About description

Copy this into the **Description** field:

```
React + Vite fashion styling demo: upload an outfit photo and generate four AI looks using Cloudinary gen_replace, gen_background_replace, gen_restore, gen_recolor, signed upload, and eager transformations.
```

## Topic tags

Add these topics (edit as needed if GitHub rejects duplicates):

```
cloudinary
generative-ai
gen-replace
gen-recolor
gen-background-replace
gen-restore
image-transformation
react
vite
express
typescript
fashion
ecommerce
url-gen
eager-transformations
```

## Update via GitHub CLI

If you have the [GitHub CLI](https://cli.github.com/) installed and authenticated:

```bash
gh repo edit cloudinary-devs/Cloudinary-FashionistaAI \
  --description "React + Vite fashion styling demo: upload an outfit photo and generate four AI looks using Cloudinary gen_replace, gen_background_replace, gen_restore, gen_recolor, signed upload, and eager transformations." \
  --add-topic cloudinary \
  --add-topic generative-ai \
  --add-topic gen-replace \
  --add-topic gen-recolor \
  --add-topic gen-background-replace \
  --add-topic gen-restore \
  --add-topic image-transformation \
  --add-topic react \
  --add-topic vite \
  --add-topic express \
  --add-topic typescript \
  --add-topic fashion \
  --add-topic ecommerce \
  --add-topic url-gen \
  --add-topic eager-transformations
```

## Update via GitHub web UI

1. Open https://github.com/cloudinary-devs/Cloudinary-FashionistaAI
2. Click the **gear icon** next to **About** in the right sidebar
3. Paste the **Description** above
4. Add each **Topic** tag from the list
5. Save changes
